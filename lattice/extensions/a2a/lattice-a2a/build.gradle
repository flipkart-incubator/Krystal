plugins {
    id 'org.kordamp.gradle.jandex' version "${kordamp_jandex_version}"
}

description = "Add A2A Server functionality to Lattice Apps"

dependencies {
    implementation project(':lattice:lattice-core')
}
