description = "Guice servlet extension for Lattice - Facilitates using Guice Servlet as the DI framework in Lattice Apps"

dependencies {
    api project(':lattice:extensions:guice:lattice-guice')

    api 'com.google.inject.extensions:guice-servlet'
}
