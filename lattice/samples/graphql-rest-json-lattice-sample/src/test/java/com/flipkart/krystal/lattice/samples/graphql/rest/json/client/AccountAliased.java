// Moved to src/main/java/.../client (see build.gradle comment) so it goes through the module's
// already-working main-scope krystalModelsGen pipeline instead of the problematic test-scope one.
package com.flipkart.krystal.lattice.samples.graphql.rest.json.client;
