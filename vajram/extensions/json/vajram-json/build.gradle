description = 'Enables support for JSON in Krystal - models can be serialized and deserialized using JSON'

dependencies {
    implementation project(':krystal-common')

    api libs.jackson.annotations
    api libs.jackson.databind
    implementation libs.jackson.datatype.guava
}
