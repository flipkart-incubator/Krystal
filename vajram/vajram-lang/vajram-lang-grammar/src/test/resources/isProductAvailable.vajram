package com.flipkart.krystal.vajram_lang.samples.products;

vajram isProductAvailable
  in (string productId)
  out bool?
  inject (AvailabilityDB availabilityDB) {

  out ~{
    availabilityDB.isAvailable(productId)
  }
}